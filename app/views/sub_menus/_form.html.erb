<%= form_with(model: sub_menu, local: true) do |form| %>
  <% if sub_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_menu.errors.count, "error") %> prohibited this sub_menu from being saved:</h2>

      <ul>
      <% sub_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon %>
  </div>

  <div class="field">
    <%= form.label :top_menu_id %>
    <%= form.text_field :top_menu_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
